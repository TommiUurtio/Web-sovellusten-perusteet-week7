export class shape {
    _x
    _y

    constructor (x,y) {
        this._x = x
        this._y = y
        this.lineWidth = _lineWidth
    }
    set setLineWidth(lineWidth) {
        this.lineWidth = lineWidth
    }
}
